<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :order %>
    <%= f.text_field :order %>
  </p>
  <p>
    <%= f.label :date %>
    <%= f.date_select :date, :order => [:month, :day, :year], :include_blank => true %>
  </p>
  <p>
    <%= f.label :shipping %>
    <%= f.select :shipping, ["Yes", "No"] %>
  </p>
  <p>
    <%= f.label :revenue %>
    <%= f.text_field :revenue %>
  </p>
  <p>
    <%= f.submit %>
  </p>

<% end %>
